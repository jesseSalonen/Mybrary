<%# Partial on vieweissä käytetty elementti, jota voidaan vapaasti käyttää viewin osana %>
<%# Tämä partial on virheviesti, joka renderöidään vain silloin, kun modelssien muodostamisessa on ongelmia %>  
<% if (locals.errorMessage != null) { %> 
    <%= errorMessage %>
<% } %> 